# =============================================================================
# CUSTOMIZE — Per-worktree environment variables.
# This file is a template. .worktree/init.sh expands ${VARIABLE} placeholders
# and writes the result to .worktree/.env.app (gitignored).
#
# Available placeholders:
#   ${WORKTREE_NAME}         — sanitized worktree directory name
#   ${BRANCH_NAME}           — sanitized git branch name (used in subdomain routing)
#   ${PROJECT_NAME}          — project name (main repo directory name or override)
#   ${MAIN_REPO_NAME}        — main repo directory name
#   ${NETWORK_NAME}          — Docker network name
#
# Only the placeholders above are expanded. Other ${VAR} references are left as-is.
# =============================================================================

# !! IMPORTANT !!
# After editing this file, run `.worktree/init.sh` to regenerate
# .worktree/.env.app. Changes here do NOT take effect
# until the template is re-expanded.

# Examples (uncomment and adapt for your project):
# DATABASE_URL=postgres://dev:dev@postgres-${PROJECT_NAME}:5432/${PROJECT_NAME}_${WORKTREE_NAME}
# REDIS_URL=redis://redis-${PROJECT_NAME}:6379/0
# APP_NAME=${PROJECT_NAME}-${WORKTREE_NAME}
# SECRET_KEY=${SECRET_KEY}

# Observability — OpenTelemetry → OTel Collector → Jaeger
OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector-${PROJECT_NAME}:4318
OTEL_SERVICE_NAME=${PROJECT_NAME}-${BRANCH_NAME}

# Object store — SeaweedFS S3 gateway (shared infra)
S3_ENDPOINT=http://seaweedfs-${PROJECT_NAME}:8333
S3_ACCESS_KEY_ID=any
S3_SECRET_ACCESS_KEY=any
S3_REGION=us-east-1
S3_BUCKET=${PROJECT_NAME}-${BRANCH_NAME}
